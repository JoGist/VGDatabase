%h2 Details about #{@movie.title}

%ul#details
	%li
		Rating:
		= @movie.rating
	%li
		Released on:
		= @movie.release_date

# #{link_to 'Back to movie list', main_path} - #{link_to 'All reviews', movie_reviews_path(@movie)}
